<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Android 11+ 查询所有已安装应用（车机桌面必需） -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- 查询音乐应用 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <package android:name="com.tencent.qqmusic" />
        <package android:name="cn.kuwo.player" />
        <package android:name="com.kugou.android" />
        <package android:name="com.netease.cloudmusic" />
        <package android:name="com.spotify.music" />
        <package android:name="com.apple.android.music" />

        <!-- 高德地图包名（悬浮修改版 + 车机版 + 手机版） -->
        <package android:name="com.autonavi.amapauto.floating" />
        <package android:name="com.autonavi.amapauto.overlay" />
        <package android:name="com.autonavi.amapauto.chenmo" />
        <package android:name="com.autonavi.amapauto.u3d" />
        <package android:name="com.autonavi.amapauto.superv" />
        <package android:name="com.autonavi.amapauto.xf" />
        <package android:name="com.autonavi.amapauto" />
        <package android:name="com.autonavi.amapauto.pad" />
        <package android:name="com.autonavi.minimap" />

        <!-- 百度地图包名（悬浮修改版 + 车机版 + 手机版） -->
        <package android:name="com.baidu.naviauto.floating" />
        <package android:name="com.baidu.naviauto.overlay" />
        <package android:name="com.baidu.naviauto.superv" />
        <package android:name="com.baidu.naviauto.xf" />
        <package android:name="com.baidu.BaiduMap.floating" />
        <package android:name="com.baidu.naviauto" />
        <package android:name="com.baidu.carlife" />
        <package android:name="com.baidu.BaiduMap" />
        <package android:name="com.baidu.map.location" />

        <!-- 腾讯地图包名（悬浮修改版 + 车机版 + 手机版） -->
        <package android:name="com.tencent.map.floating" />
        <package android:name="com.tencent.map.overlay" />
        <package android:name="com.tencent.map" />
        <package android:name="com.tencent.map.car" />
        <package android:name="com.tencent.map.lite" />
    </queries>

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 存储权限（文件管理需要） -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <!-- Android 13+ 媒体权限 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- 音量调节 -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- 蓝牙权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- 清理后台权限 -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <!-- 安装/卸载应用 -->
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- 悬浮窗权限（悬浮导航状态按钮需要） -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- 重新排序任务（导航返回桌面需要） -->
    <uses-permission android:name="android.permission.REORDER_TASKS" />

    <!-- 音乐通知监听权限（布丁方式） -->
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />

    <!-- 开机自启权限 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 应用使用统计 -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />

    <!-- 定位权限（车速检测） -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".PandaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PandaCarLauncher"
        android:requestLegacyExternalStorage="true"
        android:hardwareAccelerated="true">

        <!-- AndroidAutoSize 全局适配：设计稿基准 1200x1920 -->
        <meta-data
            android:name="design_width_in_dp"
            android:value="1200" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="1920" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.PandaCarLauncher"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- 设为桌面启动器 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AppManagerActivity"
            android:exported="false"
            android:theme="@style/Theme.PandaCarLauncher" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.PandaCarLauncher" />

        <!-- 悬浮球设置 -->
        <activity
            android:name=".FloatingBallSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.PandaCarLauncher" />

        <!-- 悬浮导航设置 -->
        <activity
            android:name=".FloatingNavSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.PandaCarLauncher" />

        <!-- 视频播放器 -->
        <activity
            android:name=".VideoPlayerActivity"
            android:exported="false"
            android:theme="@style/Theme.PandaCarLauncher"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="sensor" />

        <!-- 关于我们 -->
        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:theme="@style/Theme.PandaCarLauncher" />

        <!-- 蓝牙音乐 -->
        <activity
            android:name=".BluetoothMusicActivity"
            android:exported="false"
            android:theme="@style/Theme.PandaCarLauncher" />

        <!-- 文件管理器 -->
        <activity
            android:name=".FileManagerActivity"
            android:exported="false"
            android:theme="@style/Theme.PandaCarLauncher" />

        <!-- 小组件 -->
        <activity
            android:name=".WidgetsActivity"
            android:exported="false"
            android:theme="@style/Theme.PandaCarLauncher" />

        <!-- 应用市场 -->
        <activity
            android:name=".AppMarketActivity"
            android:exported="false"
            android:theme="@style/Theme.PandaCarLauncher" />

        <!-- 手机投屏 -->
        <activity
            android:name=".ScreenCastActivity"
            android:exported="false"
            android:theme="@style/Theme.PandaCarLauncher" />

        <!-- 一键清理 -->
        <activity
            android:name=".CleanBackgroundActivity"
            android:exported="false"
            android:theme="@style/Theme.PandaCarLauncher" />

        <!-- 壁纸设置 -->
        <activity
            android:name=".WallpaperActivity"
            android:exported="false"
            android:theme="@style/Theme.PandaCarLauncher" />

        <!-- 悬浮球服务 -->
        <service
            android:name=".FloatingBallService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- 车速监测服务 -->
        <service
            android:name=".SpeedMonitorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- 画中画Activity -->
        <activity
            android:name=".PipActivity"
            android:exported="false"
            android:theme="@style/Theme.PandaCarLauncher"
            android:supportsPictureInPicture="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation" />

        <!-- 悬浮视频播放服务 -->
        <service
            android:name=".FloatingVideoService"
            android:exported="false" />

        <!-- 悬浮地图服务 -->
        <service
            android:name=".FloatingMapService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- 悬浮导航状态服务（导航运行时显示悬浮按钮，可快速切回地图） -->
        <service
            android:name=".FloatingNavStatusService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- 音乐通知监听服务（布丁方式） -->
        <service
            android:name=".MusicNotificationListener"
            android:label="音乐控制"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- 开机自启广播接收器 -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
